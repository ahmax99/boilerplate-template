{
  "mcpServers": {
    "graphify": {
      "type": "stdio",
      "command": "graphify-mcp",
      "args": ["graphify-out/graph.json"],
      "env": {
        "PATH": "${HOME}/.local/bin:${PATH}"
      }
    }
  }
}
